apiVersion: v1
kind: Service
metadata:
  name: {{ include "pts-document-storage-service.fullname" . }}
  labels:
    {{- include "pts-document-storage-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "pts-document-storage-service.selectorLabels" . | nindent 4 }}
